# Copy to .env for local overrides (optional). Lab defaults: wpb / wpb-dev / diswallet.
# These are known-weak development values; do not use them outside local/CI.
POSTGRES_USER=wpb
POSTGRES_PASSWORD=wpb-dev
POSTGRES_DB=diswallet
